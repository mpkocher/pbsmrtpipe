jinja2
networkx
pbcore >= 1.2.10
# pbcommand needs to be updated consistently use semver. It's currently using a N-1
pbcommand >= 0.6.0,<0.7.0
jsonschema
xmlbuilder
requests
# this doesn't play well with the 2.3 SMRTAnalysis build
#semantic_version
#sphinx-bootstrap-theme
nose # Required for pbsmrtpipe.testkit
